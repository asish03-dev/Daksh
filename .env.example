# -------------------------------------------------------------
# DAKSH (दक्ष) - Environment Variables Template
# -------------------------------------------------------------
# Copy this file to '.env' or '.env.local' to customize local configuration.
# Vite exposes variables prefixed with VITE_ to client-side code.

VITE_APP_TITLE=DAKSH
VITE_APP_VERSION=1.0.0

# Optional API / Backend URL (if connecting to an external backend service)
# VITE_API_URL=http://localhost:5000/api
